[Unit]
Description=Camguard Test-Service

[Service]
Type=simple
ExecStart=${PYTHON} -u -m camguard '${RECORD_PATH}' ${GPIO_PIN} --daemonize --upload ${ARGS}
TimeoutStopSec=3

[Install]
WantedBy=multi-user.target
